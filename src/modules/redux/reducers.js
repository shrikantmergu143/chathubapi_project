const defaultState = {
    themeType: "light"
};

export const reducer = (state = defaultState, action) => {
  switch (action.type) {
    case "":
      return state;
    default:
      return state;
  }
};

export default reducer;
  